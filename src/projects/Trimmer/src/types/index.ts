export interface MarkerProps {
  duration: number,
  scrollWidth: number,
  gripWidth: number,
  color: string,
}

export interface GripProps {
  gripWidth: number,
  color: string,
  opacity: number,
  variant: string,
}

export interface TrimmerProps {
  gripWidth: number,
  color: string,
  duration: number,
  scale: number,
}
